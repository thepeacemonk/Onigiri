Manual maintenance utilities for Anki's debug console.

Because the add-on folder starts with numbers, import these modules with
`importlib.import_module()` instead of a regular `from ... import ...` statement.
